<%  if @message.errors.any? %>

<% else %>
  const messages = document.querySelector('.messages');
  messagesContainer.insertAdjancentHTML('beforeend', '<%= j render "message", message: @message%>');
  const input = document.querySelector('#message_content');
  input.value = '';
  input.blur();
<% end %>
